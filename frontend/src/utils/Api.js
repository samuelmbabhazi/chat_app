const host = "http://localhost:9000";

export const registerRouter = `${host}/api/auth/signup`;
export const loginRouter = `${host}/api/auth/login`;
export const usersRouter = `${host}/api/auth/users`;
